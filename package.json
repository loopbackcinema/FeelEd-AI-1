{
  "name": "feeled-ai-vercel",
  "version": "1.0.0",
  "private": true,
  "description": "FeelEd AI application, structured for Vercel deployment.",
  "scripts": {
    "dev": "echo 'Run the frontend dev server'",
    "build": "echo 'Vercel will build the frontend automatically'",
    "start": "echo 'Vercel will serve the application'"
  },
  "dependencies": {
    "@google/genai": "^1.27.0"
  },
  "engines": {
    "node": "20"
  }
}
